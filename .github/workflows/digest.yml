name: Design Signal Digest

# TESTING MODE: every hour at :45 to quickly evaluate post quality.
# We use :45 (not :00) because GitHub Actions sheds load at the top of each
# hour — :00 cron runs get heavily delayed or silently skipped, especially
# for private repos. Off-peak minutes (like :45) are far more reliable.
# PRODUCTION TARGET: once a day at 09:30 Kyiv (summer UTC+3) = 06:30 UTC.
#   Cron line: '30 6 * * *'
on:
  schedule:
    - cron: '54 * * * *'         # every hour at :54 past (UTC) — off-peak window
  workflow_dispatch:             # allows manual triggering from GitHub UI

# Allow workflow to commit state.json back to repo
permissions:
  contents: write

jobs:
  run-digest:
    runs-on: ubuntu-latest
    timeout-minutes: 30   # extra room for outage-recovery sleep (10 min) + retry pass

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
        with:
          fetch-depth: 1

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run digest agent
        env:
          TELEGRAM_BOT_TOKEN: ${{ secrets.TELEGRAM_BOT_TOKEN }}
          TELEGRAM_CHANNEL_ID: ${{ secrets.TELEGRAM_CHANNEL_ID }}
          TELEGRAM_USER_ID: ${{ secrets.TELEGRAM_USER_ID }}
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          CLAUDE_SCORING_MODEL: 'claude-haiku-4-5-20251001'
          CLAUDE_COMPOSING_MODEL: 'claude-sonnet-4-6'
          MIN_SCORE_TO_PUBLISH: '6'
          MAX_POSTS_PER_RUN: '3'
          DRY_RUN: 'false'
          PYTHONUNBUFFERED: '1'
        run: python -u -m src.main

      - name: Commit updated state
        # Only commits if state.json actually changed
        run: |
          if [[ -n $(git status --porcelain state.json) ]]; then
            git config user.name "design-signal-bot"
            git config user.email "actions@github.com"
            git add state.json
            git commit -m "chore: update state [skip ci]"
            git push
          else
            echo "No state changes to commit."
          fi
